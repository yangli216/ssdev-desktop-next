# SSDEV Desktop Next 迁移就绪度

## 已完成并有自动验证

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| Tauri 内部通信 | 完成 | 远程业务页使用窄 Tauri command，不启动 localhost HTTP |
| WebPlus 请求/响应 | 完成 | 保留 `serviceId`、`method`、`parameters`、`ResCode`、`ResData`；Rust、共享契约和 TypeScript SDK 共同固定四种“保证未执行”拒绝码，SDK 区分可有界退避、需等待重启和不得自动重试，不再要求业务复制魔法数字 |
| 非幂等调用协调 | 完成实现 | 兼容调用保持不变；可选 UUID v4 操作 ID 在执行前持久接纳，相同来源/请求并发或刷新只执行一次，换参数硬冲突；响应有界内存保留，账本只存域分离哈希；过期维护按下一到期时间触发而非每次 O(n) 扫描，来源级配额防止单一业务来源耗尽全局容量；崩溃后恢复 `indeterminate`/`completedWithoutResult` 并禁止自动重放，不虚假承诺物理设备 exactly-once |
| 协议独立演进 | 完成 | 业务 Web Bridge 与 controller/plugin-host 私有帧协议使用独立常量、状态和诊断字段；`getSystemInfo()` 使用向后兼容的版本化能力声明，区分接口存在、账本运行可用和停机准入，并从实现常量公开容量/保留边界；SDK 契约测试不依赖内部宿主版本 |
| 插件隔离 | 完成 | Tauri 主进程不加载第三方组件；每个插件和架构使用独立宿主进程；Windows 控制协议走随机命名、双方 PID 认证的专用管道，与插件标准输出分离 |
| x86 / x64 | 完成实现 | 两个 MSVC 目标均通过交叉编译；真实运行由 Windows 门禁覆盖 |
| DLL | 完成实现 | cdecl/system、字符串/GBK、整数、输出缓冲区、显式依赖和 `deps: ["*"]` |
| COM / OCX | 完成实现 | 独立 STA、消息泵、缓存对象、方法、属性与 BYREF 输出 |
| EXE / BAT | 完成实现 | 不使用业务输入拼接 Shell；BAT 元字符拒绝；输出内存限制为每流 1 MiB |
| 超时、背压与崩溃恢复 | 完成 | 全入口最多 8 个在途调用且不建立无界队列，饱和请求在进入插件前以 `ResCode=-32001` 快速拒绝；已接纳调用由独立任务执行到响应或超时，页面销毁只脱离等待，不会中断、重放或错配非幂等调用；同一插件串行执行槽的等待与实际 IPC 共享方法截止时间，槽位超时以 `ResCode=-32003` 保证未执行且不淘汰健康宿主；插件安装/热更新使用取消安全的按插件维护代次，以 `ResCode=-32010` 只拒绝目标插件维护期及跨代次迟到调用，无关插件继续服务；正常退出/系统退出/重启先关闭 controller 与持久协调器准入，以 `ResCode=-32002` 拒绝未执行的竞态请求，排空原生调用并等待最后的完成标记落盘，更新安装失败会同时恢复两层准入；同一插件/目录/架构的并发首调共享单航班初始化槽，只启动一个宿主，失败后冷却 1 秒再允许一次重试；启动先完成 120 秒上限的 Health 握手；已执行调用超时终止并淘汰宿主；Windows Job Object 回收宿主及其子进程；不同插件的慢启动和维护互不持有同一个插件生命周期锁 |
| 插件与项目交付信任 | 完成实现 | Ed25519 插件双重验签覆盖全部文件和 SHA-256 清单；项目导出先确定性封装并从精确候选重新解包完成路由、授权和宿主门禁，容器复制期间复算组件摘要，并公开完整包 SHA-256 与外部签名请求核对；项目部署包另以独立 `project-bundle` 用途绑定配置、本地映射、内部包和完整 ZIP 字节，正式客户端自动查找旁签并失败关闭；schema 2 信任库强制用途隔离，并以 `active`/`retired`/`revoked` 区分官方签发、计划轮换兼容和紧急阻断；正式发布只接受 active 密钥且要求每个已声明用途存在 active 替代键，运行时为兼容接受 retired 有效签名但拒绝 revoked；Release 固定使用安装包信任根，启动环境不能替换信任库或插件宿主 |
| 插件生命周期 | 完成实现 | `.ssdev-plugin` 安全解包；候选版本在签名暂存目录并行启动 x86/x64 隔离宿主，完成认证/二次验签/Health 且不执行业务方法，预检期间活动插件继续服务；成功后才进入目标插件的短维护窗口，只排空它的调用和宿主，原子切换目录、二次验签并只替换它的路由，无关插件持续可用；签名插件卸载和本地映射删除同样只维护目标插件，并以持久事务协调目录和路由，失败/崩溃/断电后幂等恢复；完整重新扫描仍使用全局维护；安装、更新、删除均无需重启；项目包先展示配置入口/来源/环境/桌面行为以及组件新增/升级/修复/替换/保留计划，并以计划摘要绑定包、Desktop 版本及当前配置/组件完整状态，再以统一提交点原子切换项目声明组件、联合路由和配置，状态漂移要求重新预检，提交前整体回滚、提交后整体完成；预检和恢复计数进入状态与诊断 |
| 远程页面权限 | 完成 | 运行时 Tauri ACL 与 Rust 命令双重授权；业务、悬浮窗、SSO 导航和本地控制使用不同最小权限集；内置页启用 CSP 与原型冻结，SSO 来源拿不到应用命令 |
| 业务来源信任 | 完成实现 | 正式构建强制 Ed25519 签名 schema 2 来源策略；授权精确到 origin/service/method 且拒绝通配，用户配置只能收窄来源、不能扩大权限；HTTP 必须由发布方显式例外；部署自检把当前业务来源与全部插件规范方法名/alias 做双向覆盖核对，并把磁盘完整插件清单与控制器激活快照精确比较，阻断服务数量不变时的版本、入口、方法、参数等漂移 |
| 部署自检与现场记录 | 完成实现 | 自动状态刷新采用不启动宿主的快速检查；Windows 手动深度自检和现场记录导出会为当前每个插件的实际 x86/x64 架构重新启动隔离候选宿主完成 Health 往返，失败阻塞交付且不触发业务方法或替换活动路由；导出的 JSON 有界、脱敏、不覆盖并明确标记为未签名现场记录，不冒充生产切换证据 |
| 桌面壳 | 完成 | 单实例、托盘、多屏业务窗口、悬浮窗、关闭确认、缩放、查找事件；子窗口支持有界尺寸/坐标和递归只读上下文，业务窗口总数固定上限为 16 |
| 快捷键策略 | 完成实现 | 只允许五种声明式内置动作；新集合注册失败会恢复旧集合；拒绝脚本字段 |
| 受控进程 | 完成实现 | 签名策略、固定参数、启动前 SHA-256 校验、无 Shell；旧任意路径仅保留不执行 |
| 配置与 SSO | 完成实现 | 原子持久化、旧路径迁移、未知字段保留；控制台可编辑/切换最多 32 个环境，并以最大 1 MiB 的普通 JSON 文件原子导入导出；导入先只读展示入口、环境/来源、受控进程、快捷键和桌面行为变更，确认标识绑定候选文件语义与当前已保存配置，确认时在安装锁内重新读取和复核，任一漂移要求重新预检，相同配置不触发替换或关闭窗口；候选配置仍受发布方签名来源策略约束；SSO 独立强制 HTTPS、禁止重定向、限制输入和 64 KiB 响应、拒绝重复/缺值参数，并以单航班阻止并发身份响应乱序打开窗口；通用错误状态可在控制台可靠找回，HTTP 业务来源例外不能放宽该边界；Release 固定使用 Tauri 标准应用目录，启动环境不能替换配置或插件根目录，也不会从启动工作目录或便携环境变量自动导入配置 |
| 开机启动 | 完成实现 | 使用 Tauri/OS 登录启动机制；仅由本地配置显式开启，保存失败会恢复原系统状态与快捷键 |
| 应用本体更新 | 完成实现 | 本地控制台触发、HTTPS 清单、目标 Desktop SemVer 的已安装插件兼容扫描、Tauri Minisign 验签和 256 MiB 流式下载上限；检查结果的确认标识绑定精确 pending 元数据、更新签名和完整插件集合，安装锁内在下载前及包验签后两次复核；不兼容或状态漂移时在停止宿主前阻断 |
| 插件仓库与清单 | 完成实现 | HTTPS 签名索引、包大小与 SHA-256 校验、显式 Desktop SemVer 范围、插件 SemVer 防降级、精确版本撤回、原子激活及本地隔离清单；发布端从已验签 `.ssdev-plugin` 自动提取身份/版本/兼容范围并前后复算大小和摘要，把已审批固定原因撤回记录确定性编码，拒绝缺失范围、重复包/URL/版本、重复撤回和安装/撤回重叠后进入统一外部签名流程；客户端只选择最高兼容未撤回版本，提示不兼容新版本及已安装撤回版本，检查结果以计划标识绑定精确目录条目、目录签名密钥、Desktop 版本和目标插件当前内容，安装前及下载后均复核漂移；密钥泄露仍以信任库 `revoked` 强制隔离 |
| 旧资产迁移审计 | 完成工具 | 只读盘点旧配置、插件、PE 架构、`installRun`、脚本快捷键、业务前端资源与 HAR 中的本地 HTTP 依赖；不会加载 DLL、执行脚本或复制源码、URL、请求内容 |
| 生产切换机器判定 | 完成工具链 | 插件矩阵、迁移审计和 Windows 包分别生成绑定源码与输入摘要的不可覆盖证据；严格 Go/No-Go 汇总后，仅允许独立 `cutover-decision` 审批密钥签署 GO |
| 旧插件发布迁移 | 完成工具 | 从全新暂存目录生成规范化版本元数据、外部 Ed25519 待签材料和草稿黄金矩阵；排除旧客户端私钥封套，硬拒绝 `installRun`、缺失依赖和 PE 位数不符；导入外部签名后生成逐字节可复现包，单包 `release-check` 或多包 `release-set-check` 联合复核实际签名包、active 信任键、版本绑定、定稿矩阵及稳定摘要；`release-set-materialize` 以不覆盖、失败清理和崩溃标记方式生成实机候选目录，并逐包重建复核摘要 |
| 统一发布制品签名 | 完成工具 | 来源策略、受控进程策略、插件目录和项目部署包共享语义校验、域分隔待签请求、KMS/HSM 签名导入、精确用途验签和无覆盖封套流程；项目包支持 4 GiB 有界流式摘要且重新封包也会失败，正式 Windows 构建要求 `project-bundle` active 发布键并直接复用统一验证入口 |
| 截图 | 完成实现 | `Ctrl+Shift+C` 聚焦业务窗口截图；`Ctrl+Shift+A` 本地显示器冻结遮罩、用户框选、裁剪后回传；原始全屏图不交给业务页 |
| Windows 构建门禁 | 完成实现 | GitHub Actions 同时执行 Debug 全工作区与 Release 桌面安全回归，并调用 `scripts/test-windows.ps1` 覆盖双架构 DLL/COM/宿主回归 |
| Windows 安装包 | 完成实现 | CI 对 NSIS 在干净测试账户的真实标准数据目录执行合成旧版本原位升级、配置保留、PE/资源检查、启动诊断和卸载；正式脚本强制 Authenticode、独立更新公钥锚定、签名的全 bundle SHA-256 清单、绑定 Git 提交/锁文件/工具链的 schema 2 溯源元数据及桌面/npm/x86/x64 CycloneDX SBOM，安装包使用离线 WebView2 |
| 真实插件矩阵 | 完成工具 | `ssdev-plugin-tool matrix-check` 可先在任意开发平台离线检查草稿、复核标记、占位符、路由、精确输入、冲突和全方法覆盖；`scripts/test-plugin-matrix.ps1` 还要求批准的发布集合，逐包重建现场插件并精确比对包 SHA-256，再用真实 x86/x64 宿主比对黄金响应；执行前后复核源码/发布集合/插件/信任库/矩阵/宿主摘要，成功后以不覆盖方式输出 schema 2 环境证据 |
| 生产诊断 | 完成实现 | 默认脱敏 JSON 事件、64 KiB 单事件、5 MiB × 6 轮转上限、32 MiB 诊断包上限；含调用方脱离等待、执行槽超时、维护/退出期拒绝、持久调用可用性/数量/落盘失败和当前准入/维护状态计数，operationId、业务参数/响应/SSO/URL/路径禁止入日志并有源码门禁；Release 初始化失败按七个稳定阶段显示原生错误码、建议和日志目录，控制 WebView 15 秒未到达 Rust IPC 时单次提示并记录，控制台可直接打开固定日志目录 |
| 依赖供应链 | 完成实现 | Rust/npm 锁文件、npm 官方源与 integrity、默认禁用 npm 生命周期脚本；所有 GitHub Actions 固定完整提交 SHA，RustSec 与 npm 漏洞审计在依赖变更及每周定时执行；Windows 双目标分别执行严格 Clippy，安装包验收要求各 SBOM 不仅格式/目标正确，还必须包含持久账本、controller、原生执行、IPC、Tauri API 与 Vue 等关键组件 |

## 当前旧资产证据基线

- 只读审计确认现有旧配置样本包含 5 个 HTTP 业务来源；报告没有复制具体地址。它们必须优先迁移到 HTTPS，暂时保留的每个来源都要进入 schema 2 签名策略的单独风险评审。
- 旧 Electron 主进程源码确认曾监听本机 45121，并提供 `/desktop/notice` 与 `/desktop/cache`；旧 Go WebPlus 源码确认曾监听 7711。服务端代码存在只能证明历史能力，不能证明当前仍有浏览器或外部进程调用。
- 当前可用的旧前端样本未观察到 7711/45121 静态调用引用，但没有代表性业务 HAR、真实账号流程或外部进程清单，因此结论仍是“未观察到”，不是“已证明无依赖”。在这些证据补齐前，不交付 HTTP adapter，也不宣布可以永久删除兼容评审项。
- 当前没有生产插件目录、黄金请求/响应、旧 keymap 或完整 processes 样本；相应 ABI、安装动作、脚本快捷键和受控进程迁移仍必须以真实材料验证。

## 正式切换前需要外部输入或真实环境

1. 由组织发布系统提供 Ed25519 生产公钥，并把私钥保存在 KMS/HSM 或受控 CI 中。生产信任库至少为插件、来源策略和项目部署包准备 active 用途密钥；项目交付密钥应与插件发布密钥隔离。
   同一受控签名流程还需生成精确的业务 origin/service/method、SSO 导航与外链来源策略；旧 HTTP 业务站点必须经过风险确认后单独放行。
2. 提供当前生产插件目录及代表性脱敏请求/响应；先运行迁移审计，再通过 `ssdev-plugin-tool` 建立隔离暂存、签名请求、确定性包和覆盖全部 service/method 的黄金矩阵草稿。替换占位符、清除 `draft` 标记后完成实机回归。
3. 在真实 Windows x64 设备上执行 `scripts/test-windows.ps1`，同时覆盖实际 32 位 DLL、已注册 OCX、硬件驱动和院内证书环境。
4. 提供业务前端构建产物，并在代表性账号、设备和关键流程中导出 HAR；使用 `ssdev-migration-audit --browser-assets ... --browser-har ...` 确认普通 Chrome/Edge 是否仍直连 `7711` 或 `45121`。只有 HAR 确认且调用方无法同步迁移时，才交付默认关闭、带会话挑战的独立 HTTP 兼容 adapter；Tauri 内部不依赖它。
5. 确认旧 `/desktop/notice`、`/desktop/cache` 是否仍有外部进程调用。浏览器调用纳入上述 HAR 门禁；非浏览器调用优先改为经过身份校验的命名管道，仅在调用方无法改造时保留受限 HTTP adapter。
6. 提供仍在用的 `processes` 和 `keymap.json` 样例，并先运行 `ssdev-migration-audit`，用于生成生产签名进程策略和声明式动作映射；旧版 Shell 启动与 `eval(snippet)` 不迁移。
7. 在 Windows 构建机提供代码签名证书或 HSM/KMS 签名命令，以及独立的 Tauri 更新签名私钥；CI 已覆盖合成版本升级/配置保留/启动/卸载，仍需用真实上一生产版本重复升级门禁并演练失败回滚。两类私钥均不得进入仓库。

## Go / No-Go 规则

只有以下条件同时满足才能替换旧 Electron/WebPlus：

- 所有生产插件黄金用例通过，且没有调用约定需要继续猜测 ABI。
- Windows 双架构门禁、安装升级、SSO、多窗口、截图和实际硬件端到端测试全部通过。
- 生产信任库非空，安装包和插件均完成组织签名。
- 在目标部署模式下关闭 HTTP 后，Tauri 内业务功能无回退依赖。
- 已完成分批发布与旧版本可回滚演练。
